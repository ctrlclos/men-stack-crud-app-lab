<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= planet.name %></title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <div class="container">
      <a href="/planets/" class="back-link">Back to Planets</a>
      <h1>Edit <%= planet.name %></h1>

      <form action="/planets/<%= planet._id %>?_method=PUT" method="POST">
        <div class="form-group">
          <label for="name">Name</label>
          <input type="text" name="name" id="name" value="<%= planet.name %>" required />
        </div>

        <div class="form-group">
          <label for="description">Description</label>
          <textarea name="description" id="description" rows="3"><%= planet.description || '' %></textarea>
        </div>

        <div class="form-group">
          <label for="image">Image URL</label>
          <input type="text" name="image" id="image" value="<%= planet.image || '' %>" />
        </div>

        <div class="form-group">
          <label for="type">Type</label>
          <select name="type" id="type">
            <option value="">-- Select Type --</option>
            <option value="rocky" <%= planet.type === 'rocky' ? 'selected' : '' %>>Rocky</option>
            <option value="gas giant" <%= planet.type === 'gas giant' ? 'selected' : '' %>>Gas Giant</option>
            <option value="ice giant" <%= planet.type === 'ice giant' ? 'selected' : '' %>>Ice Giant</option>
            <option value="dwarf" <%= planet.type === 'dwarf' ? 'selected' : '' %>>Dwarf</option>
          </select>
        </div>

        <div class="form-group">
          <label for="diameter">Diameter (km)</label>
          <input type="number" name="diameter" id="diameter" value="<%= planet.diameter || '' %>" />
        </div>

        <div class="form-group">
          <label for="distanceFromSun">Distance from Sun (million km)</label>
          <input type="number" name="distanceFromSun" id="distanceFromSun" value="<%= planet.distanceFromSun || '' %>" />
        </div>

        <div class="form-group">
          <label for="numberOfMoons">Number of Moons</label>
          <input type="number" name="numberOfMoons" id="numberOfMoons" value="<%= planet.numberOfMoons || 0 %>" />
        </div>

        <div class="form-group">
          <div class="checkbox-group">
            <input type="checkbox" name="hasRings" id="hasRings" value="true" <%= planet.hasRings ? 'checked' : '' %> />
            <label for="hasRings">Has Rings</label>
          </div>
        </div>

        <div class="form-group">
          <div class="checkbox-group">
            <input type="checkbox" name="isFavorite" id="isFavorite" value="true" <%= planet.isFavorite ? 'checked' : '' %> />
            <label for="isFavorite">Favorite</label>
          </div>
        </div>

        <button type="submit">Update Planet</button>
      </form>
    </div>
  </body>
</html>
